---
title: Pencel
description: Write components once, output to React, Angular, Vue, and vanilla JS.
head:
  - tag: title
    content: Pencel - Component Translation Engine
  - tag: style
    content: |
      .hero-html {
        --size: 12rem;
        font-size: var(--size);
        justify-content: center;
        line-height: var(--size);
      }
template: splash
hero:
  tagline: One component, multiple frameworks
  image:
    html: ✏️
  actions:
    - text: Get Started
      link: /pencel/getting-started
      icon: right-arrow
    - text: GitHub
      link: https://github.com/max-scopp/pencil
      icon: github
      variant: minimal
---

import { Card, CardGrid, Aside } from '@astrojs/starlight/components';

<CardGrid stagger>
	<Card title="Component Translation" icon="puzzle">
		Write once in TypeScript + JSX. Transpile to React, Angular, Vue, vanilla JS.
	</Card>
	<Card title="Full Native Control" icon="document">
		Extend HTML elements directly. HTMLButtonElement, HTMLInputElement—full platform access.
	</Card>
	<Card title="Zero-VDOM Runtime" icon="rocket">
		~4 kB gzipped. Direct DOM updates. Pure Web Components output.
	</Card>
	<Card title="TypeScript First" icon="pen">
		Decorators, JSX, familiar patterns. Type-safe from start to finish.
	</Card>
</CardGrid>

## Quick Example

```tsx
import { Component, Prop, Event } from '@pencel/runtime';

@Component({ tag: 'my-button' })
export class MyButton extends HTMLButtonElement {
  @Prop() label: string = 'Click me';
  @Event() declare clicked: CustomEvent<void>;

  render() {
    return (
      <Host onClick={() => this.clicked.emit()}>
        {this.label}
      </Host>
    );
  }
}
```

<Aside>
The compiler transforms `.pen.tsx` files into framework-specific adapters automatically.
</Aside>
