---
title: Overview
sidebar:
    order: 0
---

Plugins are central to Pencel.
Core plugins provide features for component authoring, while Output plugins handle framework integration and library consumption.

By default, no core plugins are required unless you use extended language features like [Sass](./scss.md).

No output plugins are enabled by default — without a config, you only generate raw web components.

## Default Plugins

The following plugins are enabled by default to help you get started:

* [CSS](./css.md) is enabled to optimize your CSS.
