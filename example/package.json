{
  "name": "example",
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "@pencel/cli": "workspace:*",
    "ts-flattered": "link:ts-flattered"
  },
  "scripts": {
    "generate": "bun ../packages/cli/src/main.ts",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "peerDependencies": {
    "typescript": "*"
  },
  "nx": {
    "sourceRoot": "example",
    "projectType": "application",
    "targets": {
      "run": {
        "dependsOn": [
          "^build"
        ],
        "executor": "nx:run-commands",
        "options": {
          "command": "bun run generate",
          "cwd": "example"
        }
      }
    }
  },
  "dependencies": {
    "lightningcss": "^1.30.1",
    "vite": "^7.1.2"
  }
}
