{
  "name": "@pencel/web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "generate:src": " bun ../../../packages/cli/src/main.ts",
    "generate": "pencel",
    "generate:watch": "pencel --watch"
  },
  "dependencies": {
    "@pencel/runtime": "workspace:*"
  },
  "devDependencies": {
    "@pencel/cli": "workspace:*",
    "@pencel/ts-plugin": "workspace:*"
  }
}
